"""hands: record-once / replay-many computer-use automation.

An LLM discovers how to complete a task in a UI that has no API. The
successful run is distilled into a typed, versioned capability artifact.
Production invocations replay that artifact deterministically, with no
model in the decision loop.
"""

__version__ = "0.1.0"
