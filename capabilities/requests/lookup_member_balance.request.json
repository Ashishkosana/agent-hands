{
  "name": "lookup_member_balance",
  "description": "Look up a member by ID in the Fairview Teller Console and read their current savings balance.",
  "target": {
    "app": "fairview-teller",
    "entry": {
      "web": {
        "url": "http://127.0.0.1:8000/"
      }
    }
  },
  "goal_template": "Look up the member whose ID is {param:member_id} and read their current savings balance.",
  "parameters": {
    "member_id": {
      "type": "string",
      "description": "Member number as printed on statements",
      "sensitive": false,
      "example": "12345",
      "pattern": "[0-9]{1,10}"
    }
  },
  "example_params": {
    "member_id": "12345"
  },
  "identity_param": "member_id",
  "outputs": {
    "savings_balance": {
      "type": "decimal",
      "sensitive": true,
      "example_value": "$1,234.50"
    }
  },
  "outcomes": {
    "MEMBER_NOT_FOUND": {
      "description": "No member exists with this ID.",
      "params": {
        "member_id": "99999"
      },
      "identity_param": "member_id"
    }
  }
}
