# Documentation index

Start with the top-level [`README`](../README.md); it is the front door and
links into everything below. This page exists so the rest can be found in one
place.

## Maintained

| document | read it when you want… |
|---|---|
| [`DESIGN.md`](DESIGN.md) | the full design: the five invariants, the capability artifact, locator ladder, record and replay paths, failure taxonomy, escalation, safety, eval plan, and the alternatives considered and rejected. |
| [`ADAPTATION.md`](ADAPTATION.md) | how the engine was pointed at the supplied external target (MERIDIAN CORE): the seven capabilities, the `select` action, the API/dashboard/chatbot wrappers, audit controls, and the cuts. |
| [`VERIFIER_TWINS.md`](VERIFIER_TWINS.md) | the V1 outcome-verification work as built: the `UNRESOLVED` result, the read-only verifier, the pure reconciler, window attribution, chaos modes, deviations and backlog. |
| [`PRIOR_ART.md`](PRIOR_ART.md) | what the verification work is built on and what, narrowly, it adds. Read before repeating any claim of novelty. |
| [`ARCHITECTURE_DEEPDIVE.md`](ARCHITECTURE_DEEPDIVE.md) | code-anchored notes on three mechanisms: the human-in-the-loop state machine, the API request lifecycle, and hidden per-transaction tokens. |
| [`HARD_QUESTIONS.md`](HARD_QUESTIONS.md) | the hardest questions about each load-bearing piece, with the honest answers — including where the answer is "designed, not built". |
| [`diagrams/`](diagrams/) | source for the SVG diagrams used in the README (the Mermaid diagrams are inline in the README itself). |
| [`screenshots/`](screenshots/) | the screenshots used in the README, each captioned there with what it shows and what built it. |

## Measured results and evidence

| location | contents |
|---|---|
| [`../evals/results.md`](../evals/results.md) | runtime-state classification matrix, replay stability, discovery-vs-replay cost — local Fairview fixture, regenerated by `evals/run_evals.py`. |
| [`../evals/twin_results.md`](../evals/twin_results.md) | V1 verifier-twin evaluation — local fault-injection matrix and live MERIDIAN runs in separate sections, regenerated by `evals/run_twin_evals.py`. |
| [`../evidence/README.md`](../evidence/README.md) | index of the committed run records (traces, transcripts, artifacts, twin bundles). |

## Historical

[`history/`](history/README.md) holds the documents written for the August 2026
build and its external review. They are archived, not maintained.

## For AI coding assistants

[`../CLAUDE.md`](../CLAUDE.md) is the map an assistant reads before touching the
code: invariants, commands, module responsibilities, and the built/designed
line. It is kept current and is a truthful record of how the project uses
such tools.
